# Claude 项目入口

开始任何工作前，请完整阅读并遵循仓库根目录的 `AGENTS.md`。

项目规范按领域分类在 `docs/agent/`。功能状态以根 `feature_list.json` 为准，每个 pnpm workspace 在自身根目录维护 `progress.md`，跨会话交接记录在 `session-handoff.md`。

本文件只提供 Claude 兼容入口。若任何内容与 `AGENTS.md` 冲突，以 `AGENTS.md` 为唯一事实来源。
