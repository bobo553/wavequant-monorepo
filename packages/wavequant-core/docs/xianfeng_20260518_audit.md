# 先锋新材 2026-05-18 买点与第二类严格半幅审计（MONOREPO-150）

用户最终确定默认 `lecture_v3` 的第二类为浅回撤 **<1/2**。第一类既有交替资格保持；没有新增绕过全局资格的独立买点。

## 全局规则

- 默认第二类最低收盘回撤比例 `(H1-min_close)/(H1-L0) < 0.5`，等于50%不通过。命名明确带比例的其他对照方案维持各自定义。
- 已有正N后，首次跌破攻击棒虚拟防守的低开棒，若仍守住整段N起点、此前已有抵抗、阳线实体超过振幅一半、量大于前日、收盘严格超过本次N全部先前高点，可于收盘确认放量反转轧空。
- 普通低开反弹、早已跌破短线防守后复活旧N、破N起点、后续倒N以及未解除的二级压力不被豁免。仍须通过全局交替或成熟第二类资格、执行风控。
- 止损使用已知N起点，不把已破的短线防守伪装成有效止损。盈亏比过滤关闭时不据此拦截；没有开启该过滤。

## 本例证据与成交

- 按既有联合影线/收盘口径，几何N为05-11；收盘突破颈线最高4.52为05-12，两者在页面分别显示。
- 05-18低4.38破攻击防守4.44，但守N起点4.15；收盘5.10>此前抵抗高4.85，量79,253,633>前日18,961,100。
- 第二类采用已知成熟背景：L0=2.45，H1=5.99，最低收盘4.27，回撤86/177=48.5875706%，符合<50%。没有把55日短回调另外认定为新的空多交替。
- 同源真实API默认日线回退，05-18收盘参考5.10、含滑点5.10255，买入500原始股，净盈亏比过滤false。
- 06-04按已有退出规则清仓500股；06-09属于清仓后的另一笔N再入场，不按日期强制删除。
- 截至05-18的11条委托与较长历史一致，新增边界测试严格区分小于、等于和大于1/2；浏览器验证策略名称、实际买单和反转证据。

## 后续口径：V44 可选门槛

用户后续改为可选方案：默认 `lecture_v3` 恢复 `≤1/3`；上述05-18成交适用于选择 `<1/2` 的 `lecture_v3_c50`。强反转识别和全局资格规则保持，默认三分之一方案不会在05-18买入。
