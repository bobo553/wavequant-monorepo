# TradingView 可视化验收 · 2026-09-08

交付为本地只读研究工作台，不改变策略条件、回测产物或真实交易权限。

- SDK：TradingView Lightweight Charts 5.2.1，本机加载，保留署名和许可证。
- Python：`python -m unittest discover -s tests -v`，383 项通过（新增 13 项可视化接口与时点测试），运行 18.547 秒。
- 浏览器：`web/tests/e2e.cjs`，8 组通过，实际运行于 Chromium 1223，1440×1050 与 390×844。
- 浏览器证据：`results/visualization_v1/2026-09-08T10-33-56-085Z/report.json`，同目录保存桌面成交图、绩效图、窄屏图与已知订单导出。
- 图表断言涵盖：三个绩效图完整历史区间、严格默认空状态、代理 2 笔平仓、3 倍费用比较、成交定位、理论事件加载、回放排除未来退出、跨股保留截止日期、接口错误后隐藏旧数据、导航不恢复错误快照、刷新恢复、只读导出、窄屏无页面横向溢出、无运行时异常和无外部网络依赖。
- 最终重新校验三个封存运行的产物哈希，全部通过；原策略和已封存结果未被修改。

验收过程中修正：npm 包缺少 NOTICE 文件的本地引用、布尔字段解析不一致、跨股定位跳到最新日期、错误后导航可能显示旧图、窄屏 OHLC 截断，以及默认最小 K 线间距导致窄幅绩效图无法展示全历史。首次浏览器启动因本机未安装预期的 Chromium 1234 路径失败，改用实际已安装的 1223 后完成测试；未下载或覆盖用户浏览器。

## 边界

当前工作台适配已封存的原 10 股实验，尚未适配独立 62 股扩展实验。图表覆盖层是当前引擎按已选前缀重算，不宣称为旧版引擎原样重放。严格版仍为 0 笔，代理版 2 笔；可视化与工程测试通过不改变策略证据不足的结论。未接入真实下单。

## 第二次验收：图上规则与买卖点标识

补齐退出信号、规则中文映射、同日规则合并、实际价格 B/S 箭头、可知日期标注、独立图层开关、文字碰撞避让、悬浮与点击详情，以及选中事件的颈线／防守／目标参考线。未成交委托保持原始状态，不再一概写为 rejected。

- Python 全量 385 项通过，20.041 秒；新增退出信号／委托状态和 N 字点位投影回归。
- 前端纯逻辑 8 项通过，覆盖价格定位、日期、分组、六大盘态、开关、未成交语义和文字避让。
- 浏览器 10 组通过，新增实际 TradingView Canvas 点击／悬浮、点位线与独立图层开关。点击测试使用隔离合成夹具，不混入市场回测结果。
- 最新证据目录：`results/visualization_v1/2026-09-08T10-49-22-134Z/`；`annotation-selected.png` 为实际回测买入成交及点位展示，`desktop-trade.png` 为规则／信号／买卖标识总览。

## 讲义折线修正

撤回以首尾收盘及跨未知结构段虚线补齐的方案，移除 `web/polyline.js` 及其旧测试。新增独立讲义绘图层：普通段包含发展中高／低端点；四种子母连接使用 SDK 图元保留同棒两个有序顶点，并支持悬浮与点击。先母后子及十字星子母仍待规则或次级数据，不伪造连续性。策略拐点仍可切换查看，交易引擎未改。

- 新增 Python 规则测试覆盖四种子母排列、发展端点、未知路径、前缀可知性和已确认点稳定性。
- 前端投影测试保留同棒多点及缺失坐标；浏览器 11 组通过。
- 浏览器证据：`results/visualization_v1/2026-09-08T11-30-40-959Z/`，含 `lecture-drawing.png` 与 `teaching-point-click.png`。同棒点击夹具与真实行情分离；实际行情另校验三个顶点价格。
- 回归期间发现连续快速合成点击被 SDK 识别为双击，测试已将两次独立点击分开；未修改 SDK 手势行为。
- 最终回归：Python 392 项通过（18.353 秒），前端纯逻辑 10 项通过。服务重启后浏览器 11 组再次通过，最终证据目录 `results/visualization_v1/2026-09-08T11-33-26-845Z/`。三个封存运行的产物完整性复核均为 `true`。

## 普通段极值延伸修正

修复绘图层固定首根候选 H／L 导致高连高／低连低，以及用首棒阴阳初始化方向的问题。现在按第一对有方向的高低关系取方向、相反侧极值取初始点，同向延伸只更新一个发展端点。单棒和全等区间不假定方向。保留虚线无圆圈样式；未改交易引擎或封存策略。

- 新增五项测试覆盖上涨阴线、下跌阳线、等低／等高、不足以判方向、转折交替和沿途端点消除；原首段测试的错误固定高点预期同步修正。
- Python 全量 397 项通过（18.262 秒）；前端 11 项通过；浏览器 11 组通过，实际行情额外检查所有普通段无 H-H／L-L 多余连接。
- 证据目录：`results/visualization_v1/2026-09-08T12-16-54-116Z/`。仍不以测试通过宣称未定义母子情形已完整解决。

## 四种子母路径接入主线

取消已定义子母关系处分段重启的绘图实现。每组子高／子低、母低／母高、母高／母低接入同一个有序顶点数组，母线之后的普通段复用末端，继续创新高／低时仅更新发展端点。连续母线复用共同顶点。原始三点存入 `teaching_paths` 供复核，不被发展端点覆盖。

青色开关改为仅强调颜色，不删线；投影以整个主路径分配同棒横向位置，前后线段使用相同坐标。无圆点，全虚线。用户未给出的先母后子及十字星仍明确中断，不套用反向规则。该层独立于交易引擎，封存结果未改。

- Python 全量 401 项通过（30.973 秒），前端 12 项通过，浏览器 11 组通过。
- 新增四组合前后衔接、延伸、反转、连续母线共享节点，以及未定义情况不补线测试。浏览器检查强调开关不删节点，前端验证共享端点坐标。
- 实际贵州茅台前缀中 172 组子母关系均唯一归入主路径，所有顶点按 `(index, ordinal)` 严格有序。尚有 306 处未定义情况，主路径仍分为 293 段；不宣称全历史无断点。
- 浏览器证据：`results/visualization_v1/2026-09-08T13-12-16-853Z/`。三个封存运行完整性复核均为 `true`。

## 已知方向的母子内包不再切断绘图

图表层改用基础单极值解释：上涨缩头接原高至子低，下跌缩脚接原低至子高。记录 `inside_connections`、点位 `drawing_rule` 与 `intrabar_order_resolved=false`，不宣称日内两个极值先后已解；严格 `observe_polyline` 仍阻断未解内包，策略不改。十字星外包及未知初始方向继续保留明确中断。

- Python 404 项通过（19.102 秒），新增下行内包、连续内包和严格策略隔离测试；浏览器 12 组通过。
- 默认贵州茅台前缀：303 处母子内包衔接已进入主路径，原 293 个分段减至 4 段，剩余 3 处未定情形；2026-08-01 之后无剩余断点。所有顶点按日期及同棒序号有序。
- 证据：`results/visualization_v1/2026-09-08T14-50-07-065Z/`，`recent-inside-actual.png` 为真实行情最近 25 根 K 线近景，不是合成回测结果。
