# v0.4 实际验收记录（2026-09-08）

最终运行：`results/operations_v1/runs/acceptance_20260908_verified`。

- 当前源码／测试／依赖声明与该运行冻结环境一致。
- 全量 unittest：370 项通过；本轮新增 28 项。
- 通达信既有十股快照：21,011 条日线，最新 2026-09-07。
- 严格版及日线代理版分别执行滚动、原费用、2 倍／3 倍成本、半容量诊断。
- 纸面教学场景完成信号、部分成交、重启、幂等回报、对账、退出与备份恢复。
- FIFO 账户归因守恒检查通过；教学净收益不作为真实策略收益。
- 最终运行封存 360 个文件。
- 使用 CLI 备份及恢复整套运行目录，共 1,084 个文件。
- 原目录与恢复目录的三次运行均为 COMPLETED，全部历史运行封存指纹校验通过，操作事件链摘要相同。

整套备份：`results/operations_backup_20260908_verified`。

已验证恢复副本：`results/operations_restored_20260908_verified`。

前一次 `_final` 整套恢复演练暴露 SQLite 页头重排导致字节指纹不一致；已经修复并加入完整目录恢复回归测试。该旧演练目录保留用于诊断，不应当作已通过的恢复副本；使用上述 `_verified` 目录。

## 历史诊断结果与限制

原参数严格版：0 笔。日线代理：2 笔已平仓，整个历史区间组合净收益约 +0.282%，3 倍成本约 +0.227%。这不是年化收益，更不是独立前瞻证据；两笔交易不足以建立策略优势。

本轮没有改入场条件或根据收益挑参数，也没有覆盖原 v4／v5 研究结果。

全部 21,011 行缺少当时可知证券状态输入；没有官方日历，日线新鲜度为 UNKNOWN。严格折线需要的次级路径、历史完整股票池、独立标注与前瞻样本仍未具备。

实盘路由关闭。外部通知、多用户 Web 管理、长期真实策略模拟运行、券商沙箱和异地灾备不在本轮已验收范围。完整命令与边界见 `docs/operations_contract.md`。
